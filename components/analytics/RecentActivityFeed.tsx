import type { ActivityFeedItem } from "./types";
import { formatNumber } from "./utils";
export function RecentActivityFeed({ transactions, maxRows = 10 }: { transactions: ActivityFeedItem[]; maxRows?: number }) {
  return <div className="max-h-[28rem] divide-y divide-outline-variant/30 overflow-y-auto rounded bg-white shadow-elevation-1">{transactions.slice(0, maxRows).map((item) => <article key={item.transactionNumber} className="grid gap-2 p-4 md:grid-cols-[1fr_auto] md:items-center"><div><p className="font-body text-body-md">{item.itemName} <span className="font-mono text-mono-md">{item.itemCode}</span></p><p className="font-mono text-mono-sm text-on-surface-variant">{item.transactionNumber} · {item.lotNumber} · {formatNumber(item.qty)}</p><p className="font-label text-label uppercase text-primary">{item.movementType.replaceAll("_", " ")} · {item.flowType}</p></div><time className="font-body text-body-sm text-on-surface-variant md:text-right" dateTime={item.createdAt}>{new Date(item.createdAt).toLocaleString()}</time></article>)}</div>;
}
