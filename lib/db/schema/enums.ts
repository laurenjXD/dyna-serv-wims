// Placeholder — Phase 0 scaffolding only.
//
// This file exists so `database-builder` fills in an existing skeleton file
// rather than creating one from scratch. Full enum definitions
// (party_role, flow_type, location_type, lot_status, wrr_status,
// pick_list_status, movement_type, conformance_status,
// non_conformance_reason, commitment_status) are specified in
// specs/01-core-data-model/design.md §1.1 and are implemented as a
// dedicated, reviewed task — not part of this scaffolding pass.
export {};
