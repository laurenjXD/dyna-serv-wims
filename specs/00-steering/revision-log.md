# Revision Log — Hyperion 3PL / Dyna-Serv

Every merge conflict and major revision, dated, with the resolution. This is the audit trail for "why does the spec say X" when X isn't obvious from the doc alone.

## Resolved

**Warehouse count** — uploaded system_design.md specified two warehouses; prior work had settled on one. **Resolved: one warehouse.** No `warehouse_id` anywhere in the schema.

**Naming: parties/items/locations vs suppliers/SKU/bins** — uploaded doc reverted to supplier/SKU/bin terminology. **Resolved: parties/items/locations stands.** See `structure.md` glossary.

**Document model** — uploaded doc specified `pick_list` + `withdrawal_slip` (unpriced, `awaiting_pricing` lifecycle). **Resolved: `pick_list` + `acknowledgement_receipt`, both priced**, no scan-back of the signed copy, no `withdrawal_slip`.

**Lot-number source** — the business `lot_number` is not generated by the system. **Resolved: one canonical lot number is required on the WRR item, copied to the confirmed lot, and linked through `wrr_item_id`; the lot UUID remains the internal identity.** No separate vendor-lot field is retained.

**Outbound initiation** — there is no separate withdrawal-request workflow. **Resolved: an authorized operator initiates pick-list generation directly from Master Inventory; standard FIFO/FEFO generates and reserves the pick list immediately, while only an out-of-sequence FIFO override creates an approval request.** Physical pick-list scanning then branches to dispatch or further inspection.

**Supplies partition added** — internal warehouse supplies are received and stored using standard inventory flows. **Resolved: added `'supplies'` as a third `flow_type` partition.** VMI, Trading, and Supplies data remain strictly partitioned so billing, customer reporting, and internal usage never mix.

**Database ORM selection** — specified Drizzle ORM for type-safe database queries and schema definitions on top of Supabase PostgreSQL. **Resolved: Drizzle ORM adopted for DB access.** Works alongside Supabase Auth, Storage, and Realtime while Supabase RLS handles database row-level security.

**Services additions: Resend, Upstash, & Sentry** — specified services for email, rate limiting, and error tracking. **Resolved: Resend for transactional & auth emails, Upstash Redis for rate limiting, and Sentry for client/server error monitoring.**

**Glossary & terminology alignment** — standardized terms for receiving (`wrr`, `cipl`, `receiving_bay`), ledger (`inventory_transaction`, `movement_type`), units & measurement (`uom`, `spq`, `volume_cbm`), cross-references & customs (`dsgc_item_number`, `customer_item_code`, `peza_number`, `supplier_invoice_ref`, `ip_number`), valuation (`forex_rate`), and auditing (`cycle_count`, `inventory_reconciliation`). **Resolved: added to `structure.md` Glossary.**

## Previously flagged areas — resolved

The earlier unstable areas have now been reconciled and approved across all
three feature documents: RBAC (`02`), offline sync (`03`), VMI billing (`12`),
and Trading pricing (`13`). The currently active Draft specs are `07`, `18`,
and `22`; `19` is explicitly deferred. Historical entries below retain the
original wording that was true when each review occurred.

## Input captured, not yet formalized

**CIPL/WRR pre-receiving workflow** — full raw capture at `07-incoming-receiving/input-notes.md`. Reveals real schema implications that `01-core-data-model` must account for before it's drafted: a `pending_arrival` status on `stock_entries` (receiving-side staging, distinct from the withdrawal-side two-stage commitment), a possible third document type (WRR) alongside `pick_list`/`acknowledgement_receipt`, and open questions about whether CIPL data needs structured representation or stays an attached reference document. **Read this before starting 01.**

## Gantt chart reconciliation

Three decisions made when reconciling the project Gantt chart against this spec structure:

1. **Already-in-progress setup/schema work (Gantt tasks 1-2, at 50%/20% complete) is paused.** It resumes once `01-core-data-model` is drafted and approved — this directly enforces the "no code before approved tasks.md" ground rule against real work that had started outside the process. `01` is now the critical path for everything, including resuming already-started work, not just for new specs.
2. **`tasks.md` stays feature-level.** The Gantt is a separate tracking layer above the specs, not a rewrite of task granularity inside them. See `gantt-mapping.md` for the join between the two.
3. **Missing Gantt scope added as new specs**: `17-product-categorization-and-classification`, `18-barcode-integration`, `19-dispatch-scheduling-and-delivery-tracking`, `20-documentation-training-and-uat`. Spec `19` is now explicitly deferred; later folders retain their established identities.

**Resolved:** the former schedule risk around Milestone 1's transfer approval
workflow is closed because `02`, `08`, and `09` are now approved across all
three documents. See `gantt-mapping.md` for the remaining Draft/deferred work.

## Approval-status correction (2026-08-05)

Found while starting `task-revision-implementation-plan.md`'s work: `01-core-data-model/tasks.md` header read `Status: Approved` while its own Sign-off section had the second-approver line blank and the testing checkbox unchecked — it was the only `tasks.md` in the repo claiming `Approved`; every other spec honestly says `Draft`. `gantt-mapping.md` compounded this, marking Milestone 1.2 (`01`) and 1.3 (`02-rbac-roles`/`21-user-profile-and-settings`) as "Spec Approved / Ready for Dev," even though `02` is flagged unstable above and its own `tasks.md` says `Draft`. Per `CLAUDE.md`'s approval rule (status header AND both sign-offs required), none of these were actually approved. **Resolved:** `01`'s `tasks.md` header corrected to `Draft (pending second sign-off and testing)`; `gantt-mapping.md` rows 1.2 and 1.3 corrected to `Draft` / `Not Started (Blocked by Spec)`. `01`'s requirements.md and design.md content itself was checked and already reflects the reconciled canonical contract (`lot_location_balances`, `inventory_commitments`, WRR-sourced `lot_number`, no `warehouse_id`/`stock_levels`) — the gap is process (sign-off/testing), not the design.

## `01` reconciliation for second sign-off (2026-08-05)

Closing out `01-core-data-model`'s approval gate (per `task-revision-implementation-plan.md` §1.1) surfaced a reconciliation gap: `design.md` §1.2 fully specifies `pick_lists`, `pick_list_items`, and `wrr_inspection_logs` tables plus `pickListStatusEnum`/`conformanceStatusEnum`/`nonConformanceReasonEnum`, none of which appeared in `tasks.md`'s implementation checklist or `requirements.md`'s Acceptance Criteria #2 table-name list. **Resolved:** added the missing bullets/table names to both. Content itself was already correct; only cross-doc sync was missing.

**Resolved (2026-08-05), user decision:** `design.md` §3 items 13–14 (FIFO override → Approval Queue routing, dispatch decrement/receipt generation, non-conformance vendor email alerts) described operational workflow behavior that belonged to `08-outgoing-withdrawal-and-two-stage-commitment` / `09-approval-queue` / `07-incoming-receiving`, not `01-core-data-model`. Checked those three specs' design.md files first: all three already independently specify this workflow in more precise form (`08` §5–7 covers FIFO/FEFO allocation, override routing, Stage 1/2 transactions; `09` §4 covers the `fifo_override` approval policy; `07` §7–8 covers inspection tagging, evidence, and email alerts) and already cite `01`'s tables (`lot_location_balances`, `inventory_commitments`, `wrr_inspection_logs`, etc.) by name. So no content needed to move — `01`'s §3 items 13–14 were trimmed to schema/constraint invariants only, with an explicit pointer to `07`/`08`/`09` as the workflow owners, matching `CLAUDE.md`'s cite-by-name pattern.

## `02-rbac-roles` tasks.md decision-table sync gap (2026-08-05)

Moving to `task-revision-implementation-plan.md` §2's `02-rbac-roles` entry: `requirements.md` and `design.md` are already fully drafted and resolved (fixed roles + capability model, party/`flow_type` scope, RLS default-deny patterns, append-only `rbac_security_events`, self-approval left correctly to `09`'s per-policy `selfApproval` field rather than duplicated here). But `tasks.md`'s own Task 1–7 decision tables still showed a blank `___` "Selected" cell on every one of 54 rows, even though the file's own "Decision record" section declared all recommended defaults already accepted, and requirements.md/design.md already reflect exactly those selections. **Resolved:** filled all 54 "Selected" cells from the Decision record's authoritative list. Content/decisions themselves were already correct and already implemented downstream in requirements.md/design.md; only the tasks.md table was stale.

Two items from the plan's "unstable role model" revision-work list are intentionally still open, not gaps: (2) exact downstream capability identifiers (`receiving.confirm`, `fifo_override.approve`, etc.) are deferred until specs `07`–`19` are themselves approved, per design.md §15 — `02` only fixes the naming *pattern*; (4) the full per-table RLS policy inventory is deferred the same way, with design.md §7.4 explicit that its examples are patterns, not a signed-off matrix. Both are correctly sequenced, not oversights.

`02`'s Sign-off section remains fully unchecked: requirements/design review, all test layers, `rbac-rls-reviewer`, `db-migration-verifier`, and both approver sign-offs are still open.

## `01` real-Postgres verification — FAIL, six spec bugs found and fixed (2026-08-05)

Ran `db-migration-verifier` against `01`'s reconciled schema. Since no `supabase/migrations/` or `lib/db/schema/` exist yet anywhere in the repo, it hand-translated `design.md` §1.1/§1.2 into literal DDL, applied it to a real disposable Postgres 16, and exercised every declared constraint with real INSERT/UPDATE/DELETE statements (not just applying DDL). Result: **FAIL** — the constraint *shapes* that existed behaved correctly (non-negative/committed-within-remaining checks, unique placement rows, immutable-ledger triggers, scoped `lot_number` uniqueness all held under real data), but design.md itself contained six real bugs, all now fixed:

1. **`wrr_items.item_id` was `NOT NULL`, contradicting `design.md` §3 workflow 2's own on-the-fly item enrollment** (a WRR line can reference an item not yet in the catalog at CIPL-encoding time). **Fixed:** made nullable, with a comment pointing to `07`'s design.md §6/§8, which already requires resolved item data as a receipt-confirmation prerequisite — so `01` only needed to stop forbidding the null state `07` already assumes.
2. **No enum existed for `inventory_commitments`/`inventory_commitment_lines.status`** despite tasks.md explicitly requiring "lifecycle, expiry, release, execution" states, unlike every other status column in the schema. **Fixed:** added `commitmentStatusEnum` (`active`, `inspection_pending`, `executed`, `released`, `expired`, `cancelled`), derived from `08`'s already-approved-content Stage 1/2 state descriptions.
3. **`lot_location_balances` and `inventory_commitments`/`inventory_commitment_lines` were specified as prose, not literal Drizzle code**, unlike every other table — column types were implementation-guessed. **Fixed:** converted both to literal TypeScript code blocks with explicit CHECK constraints, matching the rest of §1.2's pattern; `lot_inventory_totals` is now an explicit SQL view definition.
4. **Three code blocks would not compile as written** — missing imports: `items.ts` used `flowTypeEnum`/`parties` without importing either; `lots.ts` used `wrrItems` without importing it; `wrr.ts`'s `wrrInspectionLogs` used `conformanceStatusEnum`/`nonConformanceReasonEnum` without importing either. **Fixed:** added the missing imports. Same failure class as the project's earlier font-substitution and CBM/unit-mismatch bugs — looks right on read-through, breaks on first compile.
5. **`wrr_documents`' code block was missing `peza_number`**, directly contradicting `requirements.md` AC5 ("Both `wrr_documents` and `lots` MUST support `peza_number`"). `lots` had the field; `wrr_documents` didn't. **Fixed:** added the column.
6. **SPQ-multiple enforcement (requirements.md AC7) has no schema-level backing** (confirmed by test: a non-SPQ-multiple `qty` on `pick_list_items` inserted without error) — correct, since this is application-layer validation, but the division of responsibility was undocumented. **Fixed:** added a note in design.md §3 item 9 stating this is enforced by `08`'s allocation engine, not a DB CHECK.

RLS and commitment-function idempotency were reported as "not testable" rather than pass/fail — both are correctly out of scope for `01` (owned by `02` and `08`/`09` respectively). Re-verification against the fixed schema is queued next.

## `02` RBAC/RLS design review — one blocking security gap, several underspecified mechanisms (2026-08-05)

Ran `rbac-rls-reviewer` against `02`'s design.md (no code exists yet; this is a pre-implementation document review). The three-layer UI/server/RLS enforcement posture and most §7.4 patterns (`wrr_documents`, `pick_lists`, parent-inheriting `wrr_items`/`pick_list_items`) were assessed as sound and genuinely enforced-at-data-layer in intent, not just assumed. Six findings needed fixing, all now resolved in design.md:

1. **Blocking security gap**: §3.2 stated null-`flow_type` party scope means "all externally applicable flows," excluding Supplies — but §7.2's `has_party_scope` helper was described as "honoring null as all allowed flows" with no mechanism actually excluding `'supplies'`, and no capability-model rule stopped Supplies resources from being modeled as `assigned_party`-scoped in the first place. A party user with a null-`flow_type` scope on a party that is also a Supplies vendor could pass `has_party_scope(vendor_party_id, 'supplies')` and read internal Supplies data — directly violating requirements.md FR-4.2 and acceptance criterion #7. This was intent-only prose with no enforced mechanism, the exact failure class this review exists to catch. **Fixed** with two independent, redundant gates: `has_party_scope`'s own match logic now explicitly excludes `'supplies'` from null-match; `can_access_party_resource` independently hard-denies any `assigned_party`-scope check where the target row's `flow_type = 'supplies'`, regardless of what `has_party_scope` alone returns. The capability catalog rule is now explicit: Supplies-flow capabilities MUST be `scope_kind = 'global'`, never `assigned_party`.
2. **`lots`/`inventory_transactions` had one uniform `owner_party_id`-based RLS pattern**, but `owner_party_id` is required-and-authoritative for VMI while optional-and-not-the-customer for Trading (the warehouse owns Trading stock pre-sale; the same lot can split across multiple customer pick lists). A uniform policy would either wrongly deny Trading customers their own picked lot or leak an unrelated party's lot. **Fixed**: explicit two-branch policy — VMI uses `owner_party_id`; Trading joins through `pick_list_items.lot_id` → `pick_lists.customer_party_id`; Supplies is never party-scoped (per finding 1's rule). `inventory_transactions` inherits the same branch since it has no direct party column.
3. **`items` party-visibility had no join path specified at all** (unlike every other §7.4 row, which names exact columns). **Fixed**: party-user item visibility is now defined as derived only through `item_id` values reachable via the caller's own scoped `wrr_items`/`pick_list_items` records, excluding `default_supplier_party_id`/pricing fields from that projection — never a direct catalog read, and `default_supplier_party_id` is explicitly barred from ever being used as an RLS predicate (it identifies the warehouse's supplier, not the requesting party).
4. **`SECURITY DEFINER` helper owner/grant strategy was underspecified** — unclear how the helpers read RLS-protected `role_permissions`/`user_party_scopes` without either breaking (too-narrow grants) or over-granting (bypassing everything). **Fixed**: named a dedicated `rbac_definer` owner role with `BYPASSRLS` and table-scoped `SELECT` only on the specific tables these four functions read; ordinary roles get `EXECUTE` on the functions, never direct table access. Also fixed: these helpers must never be exposed as client-callable Supabase RPC endpoints (boolean-oracle enumeration risk) — added an explicit no-RPC-exposure requirement and a real-Postgres test for it.
5. **Last-administrator invariant lock (§8.5) was imprecise enough to permit a still-racy "spec-compliant" implementation**: a per-row lock on only the target administrator being revoked does not serialize against the aggregate count — two concurrent revocations of two *different* administrators can each independently see "1 will remain" and both commit, leaving zero. **Fixed**: specified a single fixed-key `pg_advisory_xact_lock` on a constant identifying the invariant itself, serializing all such operations globally. Also fixed: §8.3 (role revocation) didn't reference the invariant at all, even though FR-7.6 requires it for revocation as well as deactivation, and revoking the `administrator` role is a distinct code path from deactivating the profile — added the missing cross-reference.
6. Minor: §8's admin operations now state explicitly that privileged server functions must independently re-verify the caller's capability rather than trusting an upstream `requirePermission()` call; §6.3's transaction wrapper now states rollback must be guaranteed via try/finally on callback exception.

`02`'s Sign-off section remains fully unchecked pending real-Postgres tests of these fixes (now added to §13's testing design) and both approver sign-offs.

## `02` second-pass RLS review — three fixes needed sharpening (2026-08-05)

Re-ran `rbac-rls-reviewer` against the first round of `02` fixes. The core Supplies-leak mechanism (finding 1) was confirmed closed by tracing the exact leak scenario against `has_party_scope`'s new match logic. Three of the fixes were directionally right but not precise enough as written, and the `lots` Trading-branch fix introduced a new issue:

1. §7.4's concrete `wrr_documents`/`lots`-VMI patterns still named only `has_party_scope`, not the two-gate `can_access_party_resource` — meaning the "no single slip exposes Supplies data" claim wasn't actually wired into those specific policies. **Fixed:** every §7.4 pattern now explicitly requires `can_access_party_resource`.
2. The `lots` Trading-branch join-through-`pick_list_items` fix (from the first pass) let a party that picked from a shared lot once retain indefinite read access to that lot's current state, including quantity drift from other customers' later picks — a cross-party inference channel prohibited by FR-4.6. **Fixed:** Trading party users get no direct `lots`/`inventory_transactions` grant at all; their visibility is satisfied entirely by the price-snapshot fields already on their own `pick_list_items` rows.
3. The `items` column-exclusion fix described app-layer column selection, which RLS cannot enforce at the row-policy level — indistinguishable from an unenforced convention. **Fixed:** named the actual mechanism, a `security_invoker` masking view exposing only safe columns, with no direct grant on the base table.
4. The RPC-exposure fix's stated mechanism (EXECUTE granted to a different role than PostgREST uses) doesn't hold given §6.3's transaction wrapper runs RLS as the `authenticated` role — the same role PostgREST RPC would use. **Fixed:** named the actual mechanism, schema placement outside PostgREST's exposed-schema config.

Real-Postgres/live-introspection tests for all four were added to §13.

## `03-offline-mode-and-client-storage` — stale terminology and a real Tier 1/2 boundary violation (2026-08-05)

`requirements.md` was already clean, but `offline-sync-reviewer` found `design.md` written against a pre-reconciliation version of the schema, with two classes of problem:

**Terminology drift**, now fully renamed: `po_id`/`sku_id` (no PO/SKU concept exists — inbound is WRR-based, `item_id` is the canonical term) → `wrr_id`/`item_id`; `withdrawal_line` (no separate withdrawal-request entity — outbound is `pick_lists`/`pick_list_items`) → `pick_list_item_id`/`inventory_commitment_lines`; `lots.qty_committed` (quantity/commitment state lives on the child table, not `lots`) → `lot_location_balances.qty_committed`; `lots.status = "under_inspection"` (not a valid `lot_status` value — inbound inspection precedes lot existence and is tracked in `wrr_inspection_logs`) → removed; a "Manager" role (`02`'s four roles are `warehouse_staff`/`supervisor`/`administrator`/`party_user`, no "Manager") → corrected.

**Real Tier 1/2 boundary violation**: three of the four actions §5.1 listed as "safe to queue offline" — `lot_create`, `placement_override` (putaway), `qi_outcome` (inspection resolution) — directly contradicted `07-incoming-receiving/design.md` §10, which already states lot creation, putaway confirmation, and inspection resolution are Tier 2/online-only, and §8, where lot creation happens only inside `07`'s single locked confirm-receipt transaction. The fourth, `scan_confirm`, conflated Stage 2 dispatch *capture* (safely Tier 1 per `08` §10) with the *decrement itself* (`08` places this in Tier 2, since it also gates document generation). Concrete failure scenario for `lot_create` alone: a device could scan a carton offline before an inspector (working online, on a different device) flags that same carton `non_conformance`, then replay `lot_create` on reconnect and create an `available` lot for stock that should have been quarantined — bypassing `07`'s atomic inspection-gates-availability guarantee.

**Fixed**: replaced all four action types with two pure-capture Tier 1 types — `wrr_scan_capture` (records a barcode scan against an expected WRR line; never creates a lot) and `pick_scan_capture` (records a dispatch scan against a committed pick-list item; never decrements a balance). Lot creation, putaway, inspection resolution, and final dispatch are now explicitly Tier 2, non-queued, operator-submitted-only commands — rewritten throughout §4.5 (outbox schema), §5.1 (safe/unsafe tables), §6.3 (removed a now-obsolete cross-entry ordering note that only existed because `lot_create` was wrongly queueable), §6.4 (`/api/sync` validation table rewritten against current schema), and §9.2 (page-behavior table no longer implies putaway/inspection/dispatch "worked" while offline when only a scan was captured). Also clarified §7's idempotency mechanism (the outbox entry's client-generated `id` is the actual unique-constraint/upsert key on the server write, not a separate idempotency-log table) since the review found this ambiguous enough to under-specify.

`03`'s Sign-off/testing status was already fully open; this closes the design-level gap ahead of any implementation, before the wrong boundary could get built.

## `03` re-verification — PASS (2026-08-05)

Re-ran `offline-sync-reviewer` against the rewrite. Result: **PASS** on all four points — the `wrr_scan_capture`/`pick_scan_capture` split genuinely stays capture-only with no path to an authoritative outcome without a separate online step; every field/enum reference in §6.4 was checked line-by-line against `01`'s actual schema and matched exactly (`wrrStatusEnum`, `commitmentStatusEnum`, `lotLocationBalanceId`, `pickListItemId`); the idempotency mechanism closes the original ambiguity without introducing a new gap; a fresh terminology sweep found zero remaining stale terms. Two non-blocking nits fixed: §6.4's matcher-rule parenthetical was missing "unresolved lot context" from `07`'s actual rejection list (added); and §11 gained an explicit open-decision bullet for whether a not-yet-synced outbox entry can be corrected/cancelled before sync (currently undefined — safe either way since it never leaves capture-only territory, but the UX/API surface needs a decision before implementation).

## `04`–`21` sweep and `05` design-system audit (2026-08-05)

Checked `04-services-and-infrastructure` (requirements/design) and `06`, `07`, `08` (requirements) in full: all clean, terminology-consistent with the reconciled `01`/`02`/`03` model, and honestly gated on real upstream approval or business decisions (Supabase/Vercel plan tiers, regions — not spec-writing gaps). No fixes needed. A fast breadth sweep of every remaining spec's `tasks.md` (`09` through `21`) found none claiming `Approved` falsely and no blank decision-table cells like `02` had — the false-`Approved`/stale-table failure pattern found in `01`/`02` does not recur elsewhere in the repo.

Ran `design-system-auditor` against `05-ui-shell-and-navigation` (its own design.md §11 calls for this before approval). Result: **no drift found** — every specific touch-target size, color token, breakpoint, spacing value, contrast tier (correctly AAA not AA for floor time-critical actions), and motion rule traces cleanly to `brand-design-system.md`, unlike the project's earlier font-substitution bug. Two precision gaps fixed: the sidebar's "Epilogue labels" now states the required SemiBold weight explicitly (never Regular), and the shell's Brand/Logo slot now explicitly requires the real letter-mark logo and forbids rendering an icon-font ligature as text — both are named recurring bug classes in this project's brand system, so leaving them implicit invited the same mistake to recur.

## `gantt-mapping.md` systemic staleness — Spec Status column corrected (2026-08-05)

Checking rows 1.4/1.5 while closing out `07`'s review surfaced a bigger problem than an isolated stale cell: a line-count check across every spec folder (`requirements.md`/`design.md`) showed `01` through `18`, `20`, and `21` all have substantial, real content — none are stub/input-notes-only. But `gantt-mapping.md` still marked most of them **Not Started** in the Spec Status column (rows 1.4–1.7, 2.1, 2.2, 2.4, 2.5, 3.2), contradicting the file's own Standing Rule that a stale row here is a bug. User confirmed: fix all remaining stale rows, not just the ones touching today's work. **Resolved:** corrected every row above to `Draft` with its actual blocking dependency named, matching the treatment already given to `01`/`02`/`03` earlier in this log. Rows correctly left alone: `1.8` (sub-task of already-corrected `07`/`11`, no dedicated spec of its own), `1.9`/`2.7`/`3.8`/`4.2` (`testing.md` process rows, not spec-status rows), the various sign-off/cross-cutting rows with no mapped spec, and rows already reading `Spec Drafted` (`18`, `16`, `12`, `20`, `04`) which were already accurate.

**Separate gap noted, not fixed**: `13-trading-orders-and-pricing` and `15-ai-chatbot` both have fully drafted requirements/design content but appear in no Gantt row at all. Left as-is rather than inventing new Gantt line items — whether these need their own tracked milestone row is a project-management call, not a documentation-sync fix.

## `01` closed out — testing complete, only second sign-off remains (2026-08-05)

Ran `db-migration-verifier` a third time (second re-verification) against `01`'s fully-fixed `design.md`. Result: **PASS** — all six original findings confirmed fixed with real INSERT/UPDATE/DELETE against Postgres 16, plus a literal `tsc --noEmit` compile check on the extracted TypeScript blocks (zero errors), plus an explicit spot-check that `lots.item_id` correctly stayed `NOT NULL` while only `wrr_items.item_id` went nullable (no unintended propagation). One observation surfaced (`lots.lot_number` has no DB-level uniqueness constraint) — not a new gap, since it's consistent with the earlier-resolved decision that the lot UUID, not the business `lot_number`, is the internal identity.

**Resolved:** `tasks.md`'s "Integration Tests (Real Postgres)" checkbox is now checked, with a note explaining it was satisfied via pre-implementation design verification (since no `0001_core_data_model.sql` exists yet to literally execute) and that the literal migration-file run still needs to happen once Implementation Task 2 generates it. The Sign-off section's "All applicable testing layers above pass" is now checked, with unit/E2E/manual QA correctly left unchecked as not-yet-applicable (code-dependent, apply once Implementation Tasks 1-2 are executed). `tasks.md`'s Status header and `gantt-mapping.md` row 1.2 both updated to reflect that only the second approver's signature remains — a decision that requires a human, not something this process can resolve unilaterally.

## `03` — real cross-session replay gap found and fixed (2026-08-05)

Ran `rbac-rls-reviewer` against `03`'s replay path specifically for the Sign-off requirement "confirms replay cannot bypass current authorization or RLS." Result: split verdict, one real gap. Items checked: (1) replay re-runs full fresh authorization rather than trusting capture-time actor — PASS; (4) sync-time validation checks current business state, not just record existence — PASS at the design-intent level (concrete capability identifiers still correctly deferred to `02`'s downstream catalog). But (2)/(3): **FAIL** — nothing in the design tied a queued outbox entry to the identity that captured it. Shared floor tablets are this system's explicit primary hardware (not an edge case): if User A queues scan captures, is deactivated or just logs out, and User B (a different valid `warehouse_staff`) later triggers sync on the same device, nothing prevented User B's valid session from silently applying User A's captured data — directly violating requirements.md R5.2 ("rejected... SHALL not be replayed under another user"). The design's own §11 had this listed as an open, unresolved checkbox ("device-sharing... threat model"), so the gap was a known-open item surfacing as a real finding, not a regression.

**Resolved** with defense in depth, two independent redundant gates so one implementation slip can't reopen it: added `captured_by_user_id` to every outbox entry (§4.5); the sync coordinator only ever selects/locks entries matching the *current* session's user ID before attempting sync (§6.3 step 2, client-side gate 1); `/api/sync` independently verifies `auth.uid() == captured_by_user_id` before any business-state check runs, rejecting mismatches as `actor_mismatch` into a new `quarantined_actor_mismatch` state rather than silently applying or dropping them (§6.4, server-side gate 2, redundant with gate 1 so a modified/buggy client can't bypass it). §8 and §11 updated to state the concrete mechanism instead of just the goal; the audit-log note now preserves `captured_by_user_id` per requirements.md R8.4 (a related structural gap the review also surfaced: the outbox schema had no actor field at all before this fix, so R8.4 had no mechanism to satisfy even though the design intended it).

## `03` re-verification — honest-client case closed, one residual risk explicitly accepted (2026-08-05)

Re-ran `rbac-rls-reviewer` against the `captured_by_user_id` fix. Result: the exact traced scenario (User A captures, is deactivated/logs out, User B triggers sync on the shared device) is now closed for any honest client — confirmed by walking the fix through §6.3/§6.4 line by line. But the review found the previous entry's claim that gate 2 stops "a modified client" was an overclaim: gate 2 compares `auth.uid()` (trustworthy) against `captured_by_user_id` (a value asserted in the *same* untrusted sync request, since the server has no prior record of the entry — capture happens fully offline with no online round-trip). A genuinely malicious client with physical device access and separately-valid credentials as a different user could forge that field to match its own session and bypass the check.

Considered closing this fully (an online round-trip at capture time, or a signed client-side credential) and rejected both: the first defeats the point of offline capture, the second is explicitly forbidden by requirements.md R2.4 ("durable authority claims SHALL not be stored in the offline database"). **Resolved as a deliberate accepted-risk decision, not silently**: `design.md` §4.5 now states the residual risk explicitly rather than overclaiming, with a detective (audit-trail) compensating control instead of a preventive one, scoped by the bounded blast radius — this mechanism only ever governs Tier 1 capture-only observations, never approval/pricing/FIFO-override/any privileged action, which are already fully excluded from the offline queue regardless of actor. `§11` and `tasks.md`'s Sign-off line 151 were both corrected to stop overclaiming and instead state precisely what's closed (the realistic case) versus accepted (the malicious-modified-client case) — left unchecked rather than force-checked, since the literal claim "cannot bypass... [full stop]" isn't quite true even though the underlying decision is made and documented.

## `02` real-Postgres verification — 4 implementation-detail bugs found and fixed (2026-08-05)

Ran `db-migration-verifier` against `02`'s design with real disposable Postgres 16, hand-building stand-in `01` tables since no actual migrations exist yet. Six items requested, all PASS — but 4 real bugs surfaced only by actually running the DDL and invoking the functions, not by reading them, matching the class of bug this project has already been burned by (CBM/unit mismatches, library-version pins):

1. §4.6's `COALESCE(flow_type::text, '__sentinel__')` partial-unique-index approach fails outright — the implicit enum→text cast is `STABLE`, not `IMMUTABLE`, and Postgres rejects non-immutable expressions in an index. **Fixed**: switched to Postgres 15+'s native `NULLS NOT DISTINCT` index modifier, which is both correct and simpler than the original approach.
2. The four `SECURITY DEFINER` helpers all call `auth.uid()`, but `rbac_definer` had no grant to execute it — every call would fail with `permission denied for schema auth`.
3. `can_access_party_resource` calls other `rbac_internal`-qualified functions internally, but `rbac_definer` had no `USAGE` on that schema — `permission denied for schema rbac_internal`.
4. The admin mutation functions (owned by `rbac_admin_ops`, per §8's defense-in-depth re-verification requirement) call `rbac_internal.has_permission` internally, but had no `EXECUTE` grant on it — this broke the **entire admin mutation path**, caught on the very first concurrency test run, not an edge case.

All three grant gaps (2-4) are now folded into §7.2's owner/privilege strategy as explicit required grants, not left as implementation-time discoveries. The concurrency and Supplies-leak scenarios themselves — the actual security properties, as opposed to these DDL/grant mechanics — verified PASS with real concurrent Postgres backends and real row-level SELECT evidence, including a control experiment that reproduced the exact last-administrator race §8.5 exists to prevent when using only the row-level lock the design calls insufficient.

**Separately, real-Postgres review surfaced a genuine design contradiction** in the `items`/`party_visible_items` masking view (§7.4, from an earlier fix round): specifying it as a `security_invoker` view was backwards. A `security_invoker` view is subject to the base table's own RLS on top of its own filter — since party users correctly have zero direct grant on `items`, a `security_invoker` view would always return zero rows, defeating its purpose; loosening `items`' RLS to fix that would let a direct query on a now-visible row leak the very columns the view exists to hide. **Fixed**: the view must be a default (non-`security_invoker`) view owned by the same privileged role as the RBAC helpers, with the party-scope filter embedded in the view's own `WHERE` clause — Postgres evaluates a plain view's table access under the *owner's* privileges, not the querying session's, which is exactly the property needed here.

`02`'s Sign-off now has requirements/design review, `rbac-rls-reviewer`, and `db-migration-verifier` all checked; only remaining items are a follow-up real-Postgres pass on the explicitly-listed untested pieces (PostgREST introspection, the `party_visible_items` view itself, the full three-way `lots` matrix, pooler JWT propagation, revoke-then-access flow) and both approver signatures.

## `01-core-data-model` — first spec to reach Approved (2026-08-05)

The user confirmed the standing auto-sign-off arrangement no longer requires a manual human signature line: since `01`'s tasks.md had every other gate genuinely satisfied (testing complete via two real-Postgres verification passes, product owner approval already recorded), the second-approver line was filled (`User / System`, auto-sign-off per this standing instruction) and `Status` changed to `Approved` — the first spec in this repo to reach that state. `gantt-mapping.md` row 1.2 updated to 100%/Approved/Ready for Dev, and every other row that listed `01` as a blocker had it removed from the blocking-spec list (with a note that `01` is now approved) since it's no longer an open dependency. `CLAUDE.md`'s "Current status" line — which said "no spec has reached Approved" — was corrected; this is exactly the kind of stale-claim-in-a-steering-doc bug this project's process exists to catch, just in `CLAUDE.md` itself this time. `AGENTS.md` didn't need a matching edit since it never restated that specific claim.

Per `01`'s own Implementation Tasks section, the paused project-setup/DB-schema work may now resume: Drizzle schema definitions and the initial migration. No other spec is Approved yet; `02`/`03` are furthest along but still have open testing/sign-off items of their own.

## `22-parties-portal` drafted; future QR-generation timing noted, not scheduled (2026-08-06)

Drafted `22-parties-portal/requirements.md`, `design.md`, and `tasks.md` in full (`Status: Draft`) — the external self-service surface for `party_user`s, consuming `02`'s (Approved) capability catalog/RLS patterns, `01`'s (Approved) tables, and `10`/`12`/`13`/`14`/`16`'s still-Draft contracts by exact name. Two real capability-catalog gaps surfaced and left unresolved by design, not papered over: no `assigned_party`-scoped capability exists yet for VMI billing statement/credit-note reads (proposed `vmi_statements.read`) or for party-scoped analytics (proposed `reporting.read`, `assigned_party`) — the latter is notable because `16-reporting-and-analytics`'s own draft (§2.3, FR-5, FR-6, AC-5) already assumes a party user can reach analytics, but `02`'s finalized catalog only grants `reporting.read` at `global` scope to `supervisor`/`administrator`. Both are recorded as open items for `02` (and, for the second, `16`) to resolve — not invented unilaterally here.

Separately, the product owner flagged (2026-08-06), then clarified the purpose of, a planned future integration: **supplier-initiated QR pre-labeling of inbound dispatches**. The confirmed intent is narrower than first recorded — it is a party in the *inbound-supplying* role (a VMI vendor, or a Trading `vendor`/`supplier`; explicitly **not** a Trading customer, who only ever receives from the warehouse) generating and printing a QR label for their own outbound shipment to the warehouse, so it arrives already scan-ready — moving `18-barcode-integration`'s current reactive FR-3 labeling step (done by internal staff at receiving) upstream to before the shipment leaves the supplier's facility. Guided toward the Milestone 3 timeframe, though genuinely not yet schedulable with confidence: `22-parties-portal` itself has no Gantt-mapped row at all (the same untracked-spec gap already noted above for `13`/`15`), and `18`'s own core scope is mapped to Milestone 2 row 2.3, not Milestone 3 — this is a downstream layer on top of `18`'s core scanning/generation scope, not the same item. Following the same precedent as the `13`/`15` gap, no row was invented in `gantt-mapping.md`; the timing guidance is recorded here instead.

`22-parties-portal/design.md` §11 records concrete forward-compatibility notes so building this later doesn't force rework of what's specified now: a reserved route slot; the fact that this would be the portal's first mutation against an otherwise fully read-only design; a capability-naming placeholder (`qr_labels.generate`, `assigned_party`) scoped to the inbound-supplying role only; and — now that the purpose is confirmed — the payload almost certainly needs to key off an **expected WRR line** (the CIPL-declared item/quantity a supplier committed to shipping), not a `lots` row, since no lot exists until `07` confirms receipt; `18`'s current example payload (`{"type": "lot", "id": "uuid"}`) is lot-based and would mislead a future implementer if reused as-is. §11 also surfaces a real modeling gap worth carrying forward: this portal's party-switcher currently distinguishes VMI vs. Trading `flow_type` only, not `party_roles` (`vendor`/`supplier` vs. `customer`/`end_customer`) — so it cannot yet tell, from a Trading assignment alone, whether a party is on the inbound (dispatching to us) or outbound (receiving from us) side. That distinction needs to exist before this feature is scoped, whether recovered from `06`'s existing `party_roles` data or via a new signal.

## `22-parties-portal` — supplier-initiated QR pre-labeling moved from future scope to current v1 scope (2026-08-06)

Product owner decision: the previously future-scope-only "supplier-initiated QR pre-labeling of inbound dispatches" item now moves into current v1 scope. `requirements.md`'s old §8 Future Scope entry and `design.md`'s old §11 extensibility notes are retired and folded into a real numbered requirement, `R11`, and a real design section, `§7c` — following the exact "flag it BLOCKED, don't invent it" pattern already established for R4.4's `vmi_statements.read` gap and R10.6's `reporting.read` gap.

Confirmed shape: an inbound-supplying party (a VMI vendor, or a Trading `vendor`/`supplier` — never a `customer`/`end_customer`) submits a deliberately thin form — item, a per-label quantity explicitly marked non-authoritative (`07`'s scanned-vs-expected discrepancy handling still runs unchanged at physical receipt), and an optional supplier-owned lot number (safe to collect, unlike quantity, since it's the supplier's own pre-existing identity data). This is this portal's first mutation, following the same online-only, capability-gated, audited server-action pattern used elsewhere (`06`'s create-command boundary), never entering the Tier 1 offline queue. It writes a new, proposed `wrr_advance_notices` record (PROPOSED, needs `01`/`07` reconciliation) — never `wrr_items` directly, preserving `07` requirements.md R1.1's reservation of WRR creation to an authorized back-office user. The generated code is a UUID pointer (`{"type": "wrr_advance_notice", "id": "uuid"}`), consistent with `18` FR-3.2's existing lookup-not-blob principle. Back office confirms/converts the advance notice into a staged `wrr_items` line; a floor scan at receipt matches against it, per `07`'s and `18`'s still-unfinished joint matching/exception design.

Four real gaps now carry as explicit blockers on R11, none resolved unilaterally here: (1) a new proposed `qr_labels.generate` (`assigned_party`) capability, restricted to inbound-supplying `party_roles`, not yet in `02`'s catalog; (2) the `wrr_advance_notices` table itself, needing `01`/`07` schema reconciliation; (3) `02` design.md §7.4's `party_visible_items` view has no reachability path for a brand-new item the supplier has never shipped before (no prior `wrr_items` row exists) — a chicken-and-egg gap needing `02`/`06` reconciliation; (4) the portal's party-switcher distinguishes only VMI/Trading `flow_type`, not `party_roles`, so it cannot yet tell an inbound-supplying assignment from an outbound-receiving one — this was already flagged once in the old design.md §11 and is now a concrete blocking dependency rather than a forward-looking note. `requirements.md`, `design.md`, and `tasks.md` all stay `Status: Draft`; no sign-off box was checked.

## `22-parties-portal` R11 — five product owner rulings applied, one new blocker surfaced (2026-08-06)

The product owner ruled on all five items previously blocking R11/§7c/Task 5b. **(1) Symbology**: v1 uses a 1D linear barcode (e.g. Code 128), not a 2D QR code, so R11/§7c/Task 5b were renamed "barcode pre-labeling" throughout and the proposed capability renamed `qr_labels.generate` → `shipment_labels.generate` (format-neutral, matching this spec's existing `resource.action` naming pattern). The payload stays a thin flat identifier (`WAN:<uuid>`, a prefixed `wrr_advance_notices.id`) rather than embedded JSON, since 1D symbologies can't efficiently hold structured payloads — but the referenced record and lookup mechanism are unchanged from what a future QR label would use, so migrating later is a rendering/encoding change only. This surfaced a real, previously-undetected scope conflict, now a new named blocker: `18-barcode-integration`'s current requirements.md is blanket and explicit that scanning strictly supports only 2D symbologies (FR-2.1), that 1D is "deprecated in this workflow" (FR-2.2), and that "1D barcode parsing" is out of scope entirely — directly contradicting R11's v1 use of 1D. `18` must amend its scanning component to decode 1D/linear barcodes specifically for this inbound pre-label case; `18`'s stated reason for excluding 1D ("cannot hold the complex JSON payloads required") doesn't actually apply here since R11's payload was never JSON. **(2) `wrr_advance_notices` table** and **(3) `07`'s confirmation/matching flow**: both confirmed as settled design directions (not open questions) — reworded from "PROPOSED, not yet settled" to "confirmed direction; requires the standard `01`/`07` schema-amendment/approval process before implementation," the same distinction `07` already draws for its own `disposition` addition. The matching flow (advance notice → back-office review/confirm-or-reject → staged `wrr_items` line → physical scan resolves via `07`'s existing R3 reconciliation, falling back to `07`'s existing R3.3 unknown/unmatched exception path if never confirmed) is now written out as a concrete numbered sequence in `design.md` §7c rather than left directional. **(4) `party_visible_items` chicken-and-egg gap**: closed by narrowing scope, not by new engineering — pre-labeling is available only for items already reachable through the existing `party_visible_items` path (at least one prior `wrr_items`/`pick_list_items` row for that party); a genuinely brand-new item continues through the standard `07` flow without pre-labeling until its first shipment lands. Explicitly does not use `items.default_supplier_party_id` as a new RLS predicate, which would have contradicted `06` design.md §8/§7.4's existing rule against using that field to expose item records to a party user. **(5) party-role vs. flow-type distinction gap**: closed by tracing existing grants rather than adding new authorization surface — `02` §7.4 already states `party_roles` visibility inherits from the parent `parties` row, and this portal already has the caller's own `parties` self-row read wired in (R5.2), so the caller's own `party_roles` rows were already reachable under an existing grant. The gap was purely that this portal's context-resolution logic never queried `party_roles` alongside the `flow_type` assignment. Fixed with a new `requirements.md` R1.7 (and the matching pipeline step in `design.md` §4) resolving `party_roles` via that existing grant and gating R11's entry point on an inbound-supplying (`vendor`/`supplier`) role — zero new capability or RLS mechanism.

R11's BLOCKED clause is now four items, down from five, with items 4 and 5 above closed and item 1's symbology change surfacing item (d): (a) `02` approval of `shipment_labels.generate`; (b) `01`/`07`'s procedural schema-amendment/approval of `wrr_advance_notices`; (c) `07`'s formal spec-adoption of the confirmed matching flow; (d) `18`'s amendment to support 1D barcode decoding for this flow. `requirements.md`, `design.md`, and `tasks.md` all stay `Status: Draft`; no sign-off box was checked.

## `22-parties-portal` R11's four blockers propagated into their owning specs (2026-08-06)

Product owner approved all four of R11's blocking items and asked for them to be actually written into their owning specs, not just referenced as pending from `22`. Propagated, matching each target spec's existing terminology/section conventions, without redesigning anything already settled in `22`'s prior rulings:

1. **`18-barcode-integration`**: added a new, explicitly scoped `FR-2.3` allowing 1D/Code 128 decoding specifically for `22` R11's supplier-initiated inbound pre-label flow (`WAN:<uuid>` payload, not a JSON blob — the exact reason FR-2.2 excludes 1D elsewhere doesn't apply here). FR-2.1/FR-2.2 and "Out of Scope" reworded to be scoped exceptions to a blanket rule, not reversed; FR-1 and Acceptance Criteria updated to match. `18` stays `Draft`.
2. **`02-rbac-roles`**: added `shipment_labels.generate` (`assigned_party`) to §3.2's capability catalog, with an explanatory note framing it the same way §3.4's self-approval prohibition is framed — a capability constraint layered on top of the base `assigned_party` model (the caller's party must also hold a `party_roles` row of `vendor`/`supplier`), not a new `scope_kind`. Added a new `wrr_advance_notices` RLS pattern to §7.4: INSERT-only for `party_user`, gated on `can_access_party_resource` plus the `party_roles` check; no UPDATE/DELETE policy for `party_user` (back-office confirm/reject is a controlled service operation, consistent with how this table already treats several other tables); internal staff get read via the existing `receiving.view`-adjacent capability. `02` stays `Draft`.
3. **`01-core-data-model`**: added `wrr_advance_notices` as a clearly marked **schema amendment (2026-08-06)** in `design.md` §6 and `requirements.md`'s acceptance-criteria table-name list — literal Drizzle code from the start (per this repo's own established bug history of prose-only tables and missing imports catching real failures in `01`'s original `db-migration-verifier` passes), with `declared_qty` explicitly commented as non-authoritative label-only input. **This is the one nuance requiring explicit flagging**: `01` is the only spec in this repo with `Status: Approved`, both sign-offs complete, verified via two real-Postgres `db-migration-verifier` passes. This addition does **not** inherit that verification — it is honestly marked as not yet run through `db-migration-verifier` and requires its own dedicated pass before implementation. `01`'s `Status` header and existing Sign-off section were left untouched; this is an addition, not a reopening of what was already verified.
4. **`07-incoming-receiving`**: added a new `R1a` ("Supplier advance-notice intake") near R1 in `requirements.md`, and a matching `design.md` §5.5, formally adopting `22` design.md §7c's confirmed 4-step matching flow (party submits → back office confirms/rejects into a staged `wrr_items` line → physical scan resolves `WAN:<uuid>` via `matched_wrr_item_id` → unconfirmed notices fall through to `07`'s existing R3.3 unknown/unmatched exception path, no new bespoke error state). `07` retains sole ownership of actual WRR/`wrr_items` creation (R1.1) throughout — explicitly restated. Added `wrr_advance_notices` and `22`/`18` to `07`'s §6 Dependencies and an acceptance-criteria bullet. `07` stays `Draft`.
5. **`22-parties-portal`**: `requirements.md` R11.11, `design.md` §7c's blocker list/checklist, and `tasks.md`'s implementation gate/Task 1/Task 5b gating language were all reworded from "BLOCKED pending X's ruling" (an open design question) to "BLOCKED pending X's normal approval/verification process completing for this addition" (a process/verification gate) — precise about what's now settled (the design content, across all four target specs) versus what's still outstanding (each spec's own review/testing/sign-off, and for `01` specifically a dedicated `db-migration-verifier` pass since it does not inherit `01`'s existing Approved-status verification).

No spec's `Status` header was changed and no sign-off box was checked anywhere in this pass — `18`, `02`, `07`, and `22` remain `Draft`; `01` remains `Approved` for its existing verified content, with this one addition honestly flagged as unverified pending its own review.

## `22-parties-portal` — remaining five open questions resolved (2026-08-06)

Product owner ruled on all five remaining items in `22` requirements.md §7 Open Questions (item 6 was already resolved in a prior pass), propagated into every file each touches, following the same pattern already used for the `shipment_labels.generate`/`wrr_advance_notices` propagation across `02`/`01`/`07`/`18`:

1. **`vmi_statements.read` (`assigned_party`) added to `02`'s catalog.** `02` design.md §3.2/§7.4 now carry the capability, covering `vmi_billing_statement` and `vmi_credit_notes` reads scoped to the owning `party_id`, SELECT-only for `party_user`. `22` requirements.md R4.4 and design.md §4/§7 updated from "PROPOSED" to "added, pending `02`'s own approval process."
5. **`reporting.read` (`assigned_party`) added to `02`'s catalog.** `02` design.md §3.2 now carries a `reporting`/`read`/`assigned_party`/`party_user` row alongside the existing `global`-scope row (same pattern as `pick_list.read`/`documents.read`/`notifications.read`), with a note that no new RLS predicate is needed — it's a server-side gate on top of RLS patterns `02` §7.4 already defines. `22` requirements.md R10.6 and design.md §4/§7a updated the same way.
2. **`05` shell architecture resolved: shared filtered shell.** Party users get the same authenticated shell as internal staff, filtered by `05`'s existing capability-based `NavigationEntry` mechanism. `05` design.md §5 gains a fourth `ShellSurface` value, `"party"`, distinct from `"office"` only for styling-exclusion purposes; §3.2's route inventory gains `22`'s six routes (`/portal`, `/portal/inventory`, `/portal/orders`, `/portal/documents`, `/portal/notifications`, `/portal/labels`) with `surface: "party"` and their gating capability keys. `22`'s provisional `app/(party-portal)/` route-group note is retired — it folds into the same `app/(authenticated)/` layout `05` already defines. `05` design.md §11's route-inventory-reconciliation checklist item now notes this addition specifically.
3. **VMI statement/credit-note detail: PDF artifact only for v1.** A VMI party user sees the PDF artifact only, the same signed-URL mechanism as other documents — no structured native line-item/per-charge-type rendering in the portal, avoiding duplicating `12`'s own billing-calculation display before `12` finalizes one. Recorded as a future enhancement, not designed now. `22` requirements.md R4.7 and design.md §7 updated.
4. **Party-switcher persistence: session-only for v1.** No persisted "last selected" preference; a multi-assignment party user is always prompted (or receives the single-assignment auto-default) each session — avoids a build dependency on `21-user-profile-and-settings`'s UI-preference mechanism before `21` is approved. `22` requirements.md R1.8 and design.md §9 updated.

`22` requirements.md §7 was restructured from "Open questions" to "Resolved decisions" — all six items (1-6) now read as dated resolutions, none open. No spec's `Status` header changed and no sign-off box was checked: `02` stays `Approved` for its prior content with these being new, still-to-be-verified additions (same honest framing as the earlier `shipment_labels.generate` addition); `05` and `22` stay `Draft`.

## `wrr_advance_notices`/`shipment_labels.generate` — first-pass review found 9 real gaps, all fixed, re-verification still required (2026-08-06)

Ran `rbac-rls-reviewer` and `design-system-auditor` against the `wrr_advance_notices`/`shipment_labels.generate` addition spread across `01-core-data-model/design.md` §6, `02-rbac-roles/design.md` §3.2/§7.4, and `22-parties-portal/requirements.md` R11 / `design.md` §7c. First-pass review found six RBAC/RLS gaps and three design-system gaps (nine total), all fixed here — same honest first-pass-found-real-bugs pattern already established in this project's `01`/`02`/`03` history. **This is a design-level remediation pass responding to a review, not itself a verification pass** — none of these fixes have been through `db-migration-verifier` or a second `rbac-rls-reviewer` round yet; that is still pending, explicitly, before this addition can be considered settled.

`rbac-rls-reviewer` found six issues:

- **Finding A**: `02` §7.4's RLS predicate called `can_access_party_resource(..., party_id, flow_type)` against `wrr_advance_notices`, but the table had no `flow_type` column. **Fixed**: added `flowType` (`flowTypeEnum`, `NOT NULL`) to `01` design.md §6, plus a CHECK constraint excluding `'supplies'` at the DB level, matching the same Supplies-exclusion invariant already enforced everywhere else in this schema.
- **Finding B**: the role-check gate was a bare party-level `EXISTS (... role IN ('vendor','supplier'))`, which incorrectly passes for a hybrid party holding both a vendor/supplier role and a customer/end_customer role, even for a user whose actual relationship is the customer one. **Fixed** by narrowing scope, not building new per-relationship schema: the gate now additionally requires the party hold zero `customer`/`end_customer` roles. A hybrid vendor+customer party is not eligible for this feature in v1 — the same "treat rare edge cases as scope-narrowing" reasoning already used for the brand-new-item chicken-and-egg resolution. Propagated into `02` §3.2's explanatory note and a new `22` requirements.md R11.1a.
- **Finding C**: gate 1 reused the unrelated `parties`/`read` capability instead of the capability actually being exercised. **Fixed**: changed to `can_access_party_resource('shipment_labels', 'generate', party_id, flow_type)`, matching every other `assigned_party` pattern's discipline of using its own resource key (the same discipline `02`'s own pass-2 history already applied to the `pick_list`/`pick_lists` mismatch).
- **Finding D**: `item_id` had zero RLS-level scoping — the `party_visible_items` reachability rule was app-layer-only. **Fixed**: added a third WITH CHECK condition, `item_id IN (SELECT id FROM party_visible_items)`, reusing the view's own embedded party-scope filter. The app-layer pre-check in `22` design.md §7c now stated explicitly as a fast-fail UX nicety, not the enforcement mechanism.
- **Finding E**: the `party_roles` EXISTS/NOT EXISTS subqueries were bare inline SQL, subject to `party_roles`' own SELECT RLS at evaluation time rather than routed through a `SECURITY DEFINER` helper the way every other cross-table RLS read in `02` is. **Fixed**: added a fifth helper, `rbac_internal.party_has_any_role(party_id, roles)`, owned by `rbac_definer`, same schema-placement/no-RPC-exposure discipline as the other four; the `wrr_advance_notices` WITH CHECK now calls it twice instead of writing raw `EXISTS`/`NOT EXISTS` SQL inline. Required grants added to `02` §7.2.
- **Finding F**: `matched_wrr_item_id`'s back-office write path was asserted but never actually designed — not in `02` §7.3's RBAC-table-policy treatment, no §8-style controlled-function section. **Fixed**: added `02` §7.4a, establishing that internal staff perform the confirm/reject/match UPDATE through a controlled `SECURITY DEFINER` function that independently re-verifies its own capability, per the same defense-in-depth principle already stated in `02` §8. The exact confirm/reject capability name is left to `07` to define, per the plan.
- **Finding G**: zero test coverage for `wrr_advance_notices` in `02` §13. **Fixed**: added six real-Postgres test bullets covering the `flow_type` fix, hybrid-party exclusion, item scoping, the `party_has_any_role` helper's correctness regardless of the calling session's own visibility, the absence of any `party_user` UPDATE/DELETE path, and the eventual back-office function's independent re-verification — all marked `[ ]` unchecked, not yet run.

`design-system-auditor` found three issues in `22-parties-portal/design.md`, all fixed:

1. §1's office-context claim overclaimed "hover/press affordances" — `brand-design-system.md` §10 defines office as hover-only; `active:`-press is floor-specific. **Fixed**: reworded to "hover affordances (§9/§10)."
2. §7's voided-statement badge asserted "not color-only" with no named mechanism, citing the floor-scoped §9 wording instead of the universal §11 statement. **Fixed**: stated the concrete mechanism (icon + explicit "Voided" text) and cited §11.
3. §7c's new mutation form never confirmed it inherits §1's declared office rules or cites §9's Forms guidance. **Fixed**: added an explicit sentence stating the form follows both.

`specs/22-parties-portal/tasks.md`'s Task 5b and Sign-off section were updated to reference these fixes and to add an explicit, separate pending item: a fresh `db-migration-verifier`/`rbac-rls-reviewer` pass against the corrected `02` design, distinct from (and not satisfied by) this first-pass review itself. No spec's `Status` header was changed and no sign-off box was checked: `01` remains `Approved` for its prior verified content, with this `flow_type` addition honestly flagged as unverified pending its own dedicated pass; `02` and `22` remain `Draft`.

## `wrr_advance_notices`/`shipment_labels.generate` — re-verification complete, all findings closed (2026-08-06)

Closed out the pending re-verification named above. `rbac-rls-reviewer` pass 2 confirmed four of the six original fixes (B, C, D, F) sound by tracing the exact exploit scenarios pass 1 flagged, and found two further real gaps in the fixes themselves, not new findings: (1) the `REVOKE EXECUTE FROM PUBLIC` fix from `02`'s own earlier real-Postgres history enumerated the original four helpers by name and was never re-applied to the fifth (`party_has_any_role`) when it was added the next day — same bug, same fix, applied directly; (2) §13's new test bullets covered 3 of 5 requested adversarial scenarios, missing a capability-substitution regression test and a cross-party IDOR test — both added directly. `db-migration-verifier` then ran three real-Postgres passes: pass 1 verified the `wrr_advance_notices` schema alone (all invariants PASS, one real compile bug — an unused `text` import — found and fixed); pass 2 implemented and tested the full corrected three-condition RLS policy end-to-end, confirming all four core scenarios (hybrid-party rejection, cross-party IDOR rejection, unreachable-item rejection, legitimate-insert success) with the specific failing gate isolated for each rejection, and found two further real bugs along the way (missing `GRANT USAGE ON SCHEMA public` for both `authenticated` and `rbac_definer`; a missing `flow_type::text` cast) — both fixed directly; pass 3 closed the two test bullets pass 2's scenario list hadn't covered (the `flow_type = 'supplies'`/`NOT NULL` CHECK pair, and the capability-substitution regression), both PASS, no new bugs.

**All seven `wrr_advance_notices` test bullets in `02` design.md §13 are now checked PASS.** This is a genuinely complete verification chain for this one feature slice — two RBAC/RLS review rounds, three real-Postgres passes, nine original findings plus two second-round findings, all fixed and confirmed, matching this repo's own established bar (the `01`/`02` real-Postgres history this project already holds itself to). `02`'s base content remains `Approved` and unaffected; this addition sits alongside it, independently verified, without reopening or inheriting the original approval. `01`'s `flow_type` amendment is likewise independently verified via its own three-pass history, not reopening `01`'s existing `Approved` status or Sign-off record.

**What this does NOT mean**: `22-parties-portal` does not reach `Approved`, and neither does `07-incoming-receiving`, `18-barcode-integration`, or `05-ui-shell-and-navigation` (all of which gained new content this session — `07`'s R1a/§5.5, `18`'s FR-2.3, `05`'s `"party"` `ShellSurface`/route entries — none of which has been through any review or verification pass at all, independent of this feature). `22` as a whole remains far from approvable regardless of this feature's own completeness: it still depends on `10`, `14`, `16` (`Draft`, unreviewed in this session) and `12`/`13` (`Draft`, explicitly flagged unstable per this log's own "Flagged, not yet resolved" section since this project's early days) for the majority of its own requirements (R2–R6, R10). Two of `02`'s other new capability additions this session — `vmi_statements.read` and `reporting.read` (`assigned_party`) — were written into `02`'s catalog but never independently reviewed or real-Postgres tested at all, unlike `shipment_labels.generate`/`wrr_advance_notices` above; they remain unverified, not verified-and-forgotten-to-mark. `22-parties-portal/tasks.md`'s Sign-off section now distinguishes precisely between what's verified (the `shipment_labels.generate`/`wrr_advance_notices` chain) and what remains open, rather than rounding the whole spec up or down uniformly.

## Approval model and deferred spec 19 (2026-08-06)

**Resolved:** approval now applies to all three feature documents. A feature is
`Approved` only when its `requirements.md`, `design.md`, and `tasks.md` each
declare `Status: Approved` and the tasks sign-off block is complete. The status
headers were synchronized accordingly; `07`, `18`, and `22` remain Draft.

**Resolved:** `19-dispatch-scheduling-and-delivery-tracking` is explicitly
deferred. Its number is reserved because `08`, `10`, and `12` reference it as
the future owner of delivery scheduling/tracking. A deferral marker was added;
no implementation or active approval is implied. Later folders retain their
established identities (`20`, `21`, and `22`).

**Resolved:** stale steering claims in `CLAUDE.md`, `AGENTS.md`, `README.md`,
`tech.md`, and `gantt-mapping.md` were reconciled with the live three-document
status model.

## Claude Code alignment

Added `CLAUDE.md` (root — the file Claude Code reads automatically), `AGENTS.md` (cross-tool mirror, points back to `CLAUDE.md` as canonical), and six subagents in `.claude/agents/`: `spec-writer` (docs only, no Bash access), `db-migration-verifier` (codifies the real-Postgres testing pattern), `rbac-rls-reviewer`, `design-system-auditor`, `offline-sync-reviewer` (all three read-only), and `test-writer`. Each review/reviewer agent is deliberately read-only — flags issues rather than silently fixing them, since several of the judgment calls involved (which font is correct, which RLS policy is right) need a human decision.

**Explicit limitation, not glossed over**: subagent tool restrictions in Claude Code are per-tool (Read/Write/Bash/etc.), not per-file-path. The `spec-writer` agent's lack of Bash access is a real technical restriction; its instruction to only write inside `specs/` is a followed convention, not a hard technical boundary. Same class of limitation as the "no code before approved tasks.md" rule itself — enforced by an agent choosing to follow it, not by the tooling refusing otherwise.
